mod plugin;

pub use plugin::QueryToScreenPlugin;

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        //let result = add(2, 2);
        //assert_eq!(result, 4);

        /*
        
        app.add_plugin(QueryToScrPlugin::config_path("./config/"));
        
        
        #[derive(Reflect)]
        struct MyStruct{


        }
        
        
        /// Somewhere
        
        fn reflet(world){



        }
        
        */
    }
}
