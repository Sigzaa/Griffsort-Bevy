use bevy::{ prelude::*};
use crate::shared::systems::*;

pub struct Logic;
impl Plugin for Logic {
    fn build(&self, app: &mut App) {
        app
        //.add_system(collect_inputs)

        ;
    }
}
