use bevy::{ prelude::*};
use super::systems::*;


pub struct GoInputs;
impl Plugin for GoInputs {
    fn build(&self, app: &mut App) {
        app
        //.add_system(collect_inputs)

        ;
    }
}
