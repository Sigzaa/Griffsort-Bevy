use super::*;
use bevy::prelude::*;
use heroes::*;

impl Plugin for Zero {
    fn build(&self, app: &mut App) {
        // app
        // .add_system(look::<Zero>)
        // .add_system(walk::<Zero>);
    }
}
